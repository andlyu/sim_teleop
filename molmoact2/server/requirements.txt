# GPU-side deps for the MolmoAct2-SO100_101 host server.
# Install on the Vast box (CUDA 12.1 base image). NOT for the Mac.
#
# torch is intentionally unpinned here — install the CUDA build that matches the
# box's driver first (the provided Vast PyTorch image usually ships it), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# MolmoAct2's remote processor currently needs the newer video helpers present
# in 4.57.x; older 4.52.x installs fail at AutoProcessor import time.
transformers>=4.57,<4.58
accelerate
einops
torchvision
pillow
numpy
protobuf
huggingface_hub
fastapi
uvicorn[standard]
pydantic>=2
